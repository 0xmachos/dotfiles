# 1Password SSH agent config
# https://developer.1password.com/docs/ssh/agent/config
#
# Keys are offered to servers in the order listed below.
# Only vaults/items listed here are served by the agent.
#
# Test: SSH_AUTH_SOCK=~/Library/Group\ Containers/2BUA8C4S2C.com.1password/t/agent.sock ssh-add -l

[[ssh-keys]]
item = "gucci"
vault = "homelab"

